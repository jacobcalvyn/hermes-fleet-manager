## Summary

## Test plan

- [ ] `make test`
- [ ] `go vet ./...`
- [ ] `cd web && npm run lint && npm run build`

## Notes

Does this change lease fencing, encryption-at-rest, Host Agent allowlists, or
the `127.0.0.1` control-plane bind? If yes, call that out explicitly.
